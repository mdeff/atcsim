-----  ATCsim DESCRIPTION  -----

ATCsim (Air Traffic Control simulation) is the 2013 project of an introduction course on object-oriented programming in C++ at EPFL (CS-118).
It simulates an (obviously simplified) Air Traffic Control workstation like the ones operators play in reality (for example at Skyguide).

-----  AUTHORS  -----

Michaël Defferrard, michael.defferrard@epfl.ch
Enguerrand Granoux, enguerrand.granoux@epfl.ch

-----  GET, BUILD and RUN  -----

  cd /path/to/project/
  git clone https://defferra@git.epfl.ch/repo/atcsim.git (GASPAR authentification)
  cd atcsim
  make
  ./ATCsim

-----  very basic GIT WORKFLOW  -----

fetch from the main repository and immediately try to merge :
  cd /path/to/project/atcsim/
  git pull (authentification GASPAR)

Edit source files, build, test, debug etc.

Commit every changes :
  git commit -a -m "commit message"

update the main (remote) git repository
  git push (GASPAR authentification)
